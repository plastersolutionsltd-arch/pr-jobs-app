name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -sf \
            -H "apikey: sb_publishable_wX0TOGQTtNWi0ex14ux6Hw_eKVge5cJ" \
            -H "Authorization: Bearer sb_publishable_wX0TOGQTtNWi0ex14ux6Hw_eKVge5cJ" \
            "https://ngakblcdnuqectjappkl.supabase.co/rest/v1/counters?select=id&limit=1"
          echo "Supabase pinged successfully"
